<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title><%= spell.name %></title>
</head>
<body>
    <header>
<nav>
    <div><h1>MySpellbook</h1></div>
    <div><a href="/"id="home">Home</a></div>

    <div>
        <form action="/sessions?_method=DELETE" method="post">
        <input class="log-out" type="submit" value="Log Out" />
    </form>
    </div>
</nav>
    </header>
    <main>
    <h1><%= spell.name %> - 
        <% if (spell.level > 0) { %>
            Level <%= spell.level %> <%= spell.school %> Spell</h1>
        <% } else { %>
            Cantrip - <%= spell.school %>
            <% } %>
        </h1>
    <section id="description">
    <h2>Description</h2>
    <p><%= spell.description %></p>
</section>
<section id="sub-description">
    <div id="range">Range: <%= spell.range %></div>
    <div id="casting-time">Casting Time: <%= spell.castingTime %></div>
    <div id="duration">Duration: <%= spell.duration %></div>
</section>
<section id="extras">
    <div id="components">
        <% if (spell.components.includes('M')) { %>
            <span>Components: </span><ul>
                <% spell.components.forEach(component => { %>
                    <li>
                        <%= component %>
                    </li>
                    <% }) %>
                    (<%= spell.materials %>)
                </ul>
        <% } else { %>
           <span>Components: </span>
            <ul>
        <% spell.components.forEach(component => { %>
            <li>
                <%= component %>
            </li>
            <% }) %>
        </ul>
            <% } %>

</div>
</section>
<div class="buttons">
    <form action="/spells/<%= spell._id %>/edit">
    <input type="submit" value="EDIT SPELL">
</form>
<form action="/spells/<%= spell._id %>?_method=DELETE" method="post">
<input type="submit" value="DELETE SPELL">
</form>
</div>
</main>
</body>
</html>